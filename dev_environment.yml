name: crazyneuraluser
channels:
  - defaults
  - conda-forge
  - pytorch
  # - fastai
dependencies:
  - python>=3.7, <3.8
  - pip

  # DEVELOPMENT ONLY PACKAGES (could also be kept in a separate environment file)
#  - jupyterlab
  - pytest
  - pytest-cov
  - tox
  - pre_commit
  - nbdime
#  - nbstripout
  - sphinx
  - recommonmark
